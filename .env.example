# Optional bootstrap admin overrides (first migration only)
BAKARR_BOOTSTRAP_ADMIN_USERNAME=admin
BAKARR_BOOTSTRAP_ADMIN_PASSWORD=change-me-strong-password
BAKARR_BOOTSTRAP_ADMIN_API_KEY=change-me-random-hex

# Optional runtime log override
RUST_LOG=info
