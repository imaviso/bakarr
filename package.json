{
  "name": "bakarr",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel --if-present dev",
    "dev:api": "pnpm --filter @bakarr/api dev",
    "dev:web": "pnpm --filter @bakarr/ui dev",
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build"
  }
}
