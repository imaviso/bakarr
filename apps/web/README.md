# Bakarr UI

Run from `apps/web`:

- `bun run dev`
- `bun run build`
- `bun run check`
- `bun run lint`
- `bun run preview`

From the repo root you can also use `bun run dev:web`, `bun run check:web`, and `bun run lint:web`.
